# Environment Variables for Elastic Beanstalk Deployment
#
# These variables are automatically provided by Elastic Beanstalk when you
# configure an RDS database instance with your environment.
#
# For local development, these are overridden in docker-compose.dev.yml
# You do NOT need to create a .env file locally.

# RDS Database Connection (provided by Elastic Beanstalk)
RDS_HOSTNAME=your-rds-instance.region.rds.amazonaws.com
RDS_PORT=5432
RDS_DB_NAME=ebdb
RDS_USERNAME=postgres
RDS_PASSWORD=your-secure-password

# Application Settings (optional)
NODE_ENV=production
